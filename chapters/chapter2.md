## Глава 2. Мета, гиперссылки и модель отображения

Язык HTML - это посредник между человеком и ~~котом~~ разного рода программным обеспечением. Достаточно часто в роли программного обеспечения выступает браузер.

Когда программа получает веб-страницу, она анализирует её структуру и контент, обрабатывает всё, а после отображает результат на экране пользователя.

Порой возникает необходимость немного повлиять на этот процесс интерпретации веб-страницы. Например, сообщить тип __кодировки__ содержимого документа или запретить поисковой системе __индексировать__ данную страницу.

Прежде мы познакомились с атрибутами, которые выполняют несколько схожую функцию - влияют на интерпретацию браузером элемента. Однако их область действия всегда связана с каким-то конкретным HTML элементом.

Сейчас же нам требуется возможность передать информацию о документе в целом. Поскольку прежде мы не сталкивались с подобной задачей, давайте наверстывать упущенное!


### Метаданные

__Метаданные__ (eng. _Metadata_) - это дополнительная информация о документе и его содержимом, которая не будет отображена на странице напрямую, однако будет использована программой, обрабатывающей наш HTML документ.

К метаданным, например, относится кодировка, так как она описывает, с помощью какой системы закодированы символы на странице. Имя создателя веб-страницы, её описание, данные о публикаторе - всё это тоже метаданные.

Упрощая, можно сказать, что _метаданные - это информация, которая описывает информацию_.

Передать метаданные силами HTML нетрудно - нужно лишь воспользоваться элементом `meta`.

```html
<head>
  <meta name="author" content="Барсиг Пушист Мурзикович">
</head>
```

Данный тег является одиночным, поэтому в закрывающем `</meta>` он не нуждается.

Также, для этого элемента действует одно важное правило: все `meta` обязательно должны быть указаны внутри элемента `head`.

Большая часть метаданных (за исключение кодировки, но об этом позднее) передаётся следующим образом:

```html
<meta name="..." content="...">
```

В атрибуте `name` указывается имя мета тега, определяющее его назначение, а `content` содержит информацию, используемую разными программами, вроде браузеров или поисковых систем.

Таких пар `name`: `content` достаточно много. Поэтому давайте остановимся на тройке мета тегов, часто используемых в повседневности.

#### Имя автора документа

```html
<meta name="author" content="Барсиг Пушист Мурзикович">
```

Данный мета тег позволяет указать одного из создателей HTML документа. Обычно здесь записывают именно имя человека, а не название организации или учреждения, разработавшего сайт.


#### Ключевые слова

```html
<meta name="keywords" content="коты, кошки, пушистый, нетолстый">
```

Уже достаточно давно поисковый алгоритм Google не учитывает значение этого мета тега, однако другие поисковики, как например китайский Baidu, вполне с ним считаются. Поэтому если вам важны разные поисковые системы, а не только Google Search, можете смело его использовать. Ключевые слова разделяются запятыми.


#### Описание страницы

```html
<meta name="description" content="Любители пушистых котов
и кошек, мы вас ждем на просторах нашего сайта! ">
```

Кратко изложите о чем ваш сайт и чему он посвящен. Не стесняйтесь использовать ключевые слова и фразы в предложениях - это позволит продвинуть вашу страницу в разных поисковых системах. Рекомендуемая длина 135-160 символов.

#### Кодировка

Метаданные, которые мы изучили, устанавливаются с помощью пары атрибутов `name` и `content`.

Но есть два исключения: HTTP заголовки и кодировка страницы. Про HTTP заголовки нам говорить немного рано, но вот кодировку упомянуть стоит.

Для того, чтобы сообщить браузеру кодировку страницы, используется элемент `meta` с атрибутом `charset`.

```html
<meta charset="utf-8">
```

Каждый HTML документ должен иметь этот мета тег, поскольку без него браузер не всегда сможет правильно декодировать содержимое страницы. В этом случае, браузер отобразит бессмысленные кракозябры, вместо привычных нам латинских и кириллических символов.

Рекомендуемой кодировкой HTML страницы является __UTF-8__.

Есть также небольшое правило касающееся метаинформации: из всех метаданных, кодировка страницы должна быть передана самой первой.


### Текстовое форматирование (Часть 2)

#### Элемент `abbr`

Порой, читая статьи или новости, Вы, наверное, сталкивались с аббревиатурами, которые должно быть знакомы бывалым читателям, но не понятны для вас. В таких случаях может выручить HTML, если точнее, его элемент `abbr` (eng. _Abbreviation_).

```html
<p>
  Задача языка <abbr title="Hypertext Markup Language">HTML</abbr> -
  разбиение тестовых данных в понятные браузеру блоки, которые можно
  связать с уже существующей информацией при помощи гиперссылок.
</p>
```

Расшифровка аббревиатуры указывается в атрибуте `title`.

При наведение на слово или фразу внутри этого тега, браузер автоматически создаст всплывающую подсказку с расшифровкой значения аббревиатуры, которую вы указали в атрибуте.

Браузеры Firefox и Opera добавят к элементу еще и подчеркивание точками.


#### Элемент `mark`

В прошлой главе мы немного поговорили о привлечении внимания читателя. Сейчас наш арсенал пополнит элемент `mark`, который позволяет выделить кусочек текста на фоне остального - браузер добавит ему желтоватый фон. Заметьте, что `mark` не говорит о важности текста, а лишь предлагает обратить на него внимание.

```html
<p>
  Если вы позовете собаку, то она, вероятно,
  тут же прибежит, виляя хвостом. Позовите кота,
  и он <mark>примет это к сведению</mark>.
</p>
```

По умолчанию браузеры добавят <mark>желтый фон</mark> для `mark`.

#### Элемент `s`

Когда информация становится не актуальной или не важной, её зачеркивают. Информация оказывается удаленной с точки зрения смысла, однако в контексте она все еще присутствует.

Хороший пример, иллюстрирующий это - цена продукта, на который объявлена скидка.

```html
CatPhone 8 всего за <s>20 000</s> 10 000 рублей!
```

Задача элемента `s` перечеркнуть текст, который сейчас (или уже) неактуален, однако нужно чтобы он остался на странице, так как информация все еще в некоторой степени важна.

Например в нашем пример, если удалить `20 000`, а не перечеркнуть, то станет не ясно, что сейчас идет акция - скидка на стоимость 50%.

Есть лишь один нюанс: не стоит использовать этот элемент, для указания информации, которая была удалена из документа или по каким-то причинам изменена. Для этих целей есть `del` и его товарищ `ins`.

#### Элементы `del` и `ins`

```html
<ul>
  <li><del cite="cats.ru/pull-requests/23">Баг #23: Не загружаются картинки на странице</del></li>
  <li><ins>Баг #24: Не загружается страница</ins></li>
</ul>
```

Как было сказано выше, информация, которая по каким-то причинам была удалена из документа, заключается в `del`.

В свою очередь информация, которую мы добавили, необходимо помещать в `ins`.

Для того, чтобы указать причину удаления, можно использовать атрибут `cite`. В этот атрибут, как и в случае с `blockquote` и `q`, указывают адрес страницы или ресурса, где есть объяснения причины удаления/добавления информации.


#### Элемент `small`

Элемент `small` был добавлен в предыдущем стандарте языка - [HTML4](https://www.w3.org/TR/html4/), и использовался для уменьшения размера шрифта текста. Однако в современном HTML - в [HTML5](https://www.w3.org/TR/html/), который мы и изучаем - этот элемент получил новую семантику.

Он по-прежнему уменьшает шрифт помещенного в него текста, однако область использования элемента изменилась: теперь в него заключают разного рода сноски и комментарии, которые косвенно связаны с основным материалом.

Обычно это небольшие комментарии автора, заметки, какие-либо предостережения или юридические ограничения, а также информация о лицензии, авторские права.

```html
<p>
    Компания FluffyCats подписала договор
    с компанией TastyFish на поставку крупных партий рыбы.
</p>
<p><small>Информация взята с сайта business.cats.ru</small></p>
```

#### Элементы `sub` и `sup`

Когда вы решитесь написать какое-нибудь математическое уравнение, или, к примеру, химическую формулу, может понадобиться добавить надстрочный или подстрочный индекс.

```html
<p>
    Химики любят H<sub>2</sub>O. Математики
    любят y = x<sup>2</sup>. А я люблю котоф.
</p>
```

Элемент `sup` иногда используют для указания сноски, содержащей объяснение термина или пояснение.

```html
<p>Знаете ли вы, что Felis silvestris catus<sup>*</sup>
  спит по 18 часов в день?</p>

<p>* - Felis catus - лат. домашняя кошка</p>
```


### Типы HTML элементов

Как вы уже наверное заметили, различные элементы разметки могут взаимодействовать между собой по-разному.

Попробуйте добавить следующий пример на вашу веб-страницу.

```html
<p>Барсиг ушел на охоту. </p>
<p>Ждать поздно.</p>

<strong>Барсиг ушел на охоту.</strong>
<strong>Ждать поздно.</strong>
```

Текст, находящийся внутри элементов `p`, выведется на разных строчках. Но тоже самое содержимое, заключенное уже в пару элементов `strong`, отобразится иначе - как будто на странице всего один `strong`.

Почему же браузер не помещает содержимое элементов `p` в одну строку, как в случае с `strong`?

Все связано с моделью отображения этих элементов. Упрощенно говоря, __модель отображения__ (eng. _Display_) - это способ, который определяет как браузер представляет элемент на странице с точки зрения занимаемого им пространства.

В HTML обычно говорят о двух моделях отображения.


#### Блочная модель

Элементы, которые отображаются согласно этой модели называются __блочными__ (eng. _Block elements_).

По умолчанию, они занимают всю ширину веб-страницы. Именно поэтому текст в элементах `p` не может быть отображен на одной строке - первый `p` выталкивает второй вниз.

Второй важной особенностью блочных элементов является то, что они могут содержать внутри себя другие блочные и/или строчные элементы.


#### Строчная модель

В этой модели элементы называют _строчными_ (eng. _Inline elements_). Размер этих элементов браузер устанавливает равным контенту, который они содержат.

Также они могут содержать только текст и другие строчные элементы.

Это значит, что мы можем вложить `strong` в `p`, но вот обратное, к сожалению, не верно.


### Гиперссылки

Гиперссылки очень важный механизм всемирной паутины, который вы используете каждый день.

Благодаря гиперссылка информация в сети может быть структурирована и "физически" связана.

Так, вы можете перейти со статьи, которая была написана неделю назад, на страницу, содержащую продолжение, выпущенное автором только вчера.

Использовать гиперссылки в HTML несложно.

```html
Возможно вы хотели бы
<a href="cats.ru/shop/">приобрести кота?</a>
```

В атрибуте `href` (eng. _Hyper Reference_) указывается ссылка на тот ресурс, на который нужно создать переход.


### Список тегов, рассматриваемых в главе

#### Метаданные

- `<meta name="author" content="Имя автора">` - имя автора документа, желательно человека.
- `<meta name="keywords" content="Ключевые слова">` - ключевые слова, разделенные запятыми.
- `<meta name="description" content="Описание страницы">` - описание страницы, желательно до 160 символов.
- `<meta charset="UTF-8">` - кодировка страницы, обычно используется UTF-8.


#### Текстовое форматирование

- `<abbr>` - содержит аббревиатуру, браузер добавляет всплывающую подсказку с расшифровкой.
   - `title= "..."` - атрибут, определяющий расшифровку аббревиатуру.
- `<mark>` - текст, на который нужно обратить внимание, браузер добавляет желтый фон.
- `<s>` - перечеркнутый текст, являющийся неактуальным или неточным.
- `<del>` - перечеркнутый текст, удаленный из документа.
- `<ins>` - новый текст, добавленный в документ
- `<small>` - косвенно связанная с остальным содержимым информация, например авторское право, комментарий автора, какие-либо предостережения. Браузером отображается уменьшенным на общем фоне шрифтом.
- `<sub>` - подстрочный индекс, например число атомов в молекуле.
- `<sup>` - надстрочный индекс, например возведение в квадрат.


#### Гиперссылки

- `<a>` - ссылка связанную страницу или какой-либо ресурс, браузер отображает синим цветом с подчеркиванием
  - `href="URI"` - атрибут, указывающий адрес перехода на страницу или ресурс

<!--- TODO: Секция "Структура веб-сайта" получится достаточно объемной. Её необходимо перенести в 3 Главу, а на занятии пройтись по ней еще раз, но уже в формате workshop.

### Структура веб-сайта

-->
